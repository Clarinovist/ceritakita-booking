# ========================================
# CeritaKita Booking App - Docker Environment
# ========================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local
#    cp .env.docker.example .env.local
#
# 2. Change ALL values below - DO NOT use these defaults in production!
#
# 3. NEVER commit .env.local to git (already in .gitignore)
#
# ========================================

# ========================================
# Admin Credentials
# ========================================
# SECURITY: Change these immediately!
# These credentials are used to access the /admin dashboard

ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMe123!SecurePassword

# ========================================
# NextAuth Configuration
# ========================================

# NextAuth URL
# - For local development: http://localhost:3000
# - For production with domain: https://booking.yourdomain.com
# - For production without SSL: http://your-server-ip:3000
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret (CRITICAL SECURITY SETTING)
# This must be a long, random string (at least 32 characters)
#
# Generate a secure secret with one of these commands:
#   openssl rand -base64 32
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# Or visit: https://generate-secret.vercel.app/32
#
# SECURITY: NEVER use the example value below in production!
NEXTAUTH_SECRET=REPLACE_THIS_WITH_A_LONG_RANDOM_STRING_AT_LEAST_32_CHARS

# ========================================
# Node Environment
# ========================================
# Keep this as 'production' for Docker deployments
NODE_ENV=production

# ========================================
# Optional: Docker Port Mapping
# ========================================
# Uncomment and change if port 3000 is already in use on your host
# Then update docker-compose.yml ports to use ${APP_PORT:-3000}:3000
# APP_PORT=3000

# ========================================
# Optional: Nginx Ports
# ========================================
# Uncomment and change if you need different HTTP/HTTPS ports
# HTTP_PORT=80
# HTTPS_PORT=443
